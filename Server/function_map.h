#pragma once
#include "precompile.h"


template<typename map, typename type> class function_map
{
public:
	function_map()
private:
	type* instance; // antar att function_map är en vänklass till type klassen och att instansen är this pekaren
};

